domains:
- domain: app-state.aikon.one
  type: PRIMARY
name: aikon-app-stage
region: ams
static_sites:
- environment_slug: html
  github:
    branch: stage
    deploy_on_push: true
    repo: alexandervu/aikon_app
  name: aikon-app-stage
  index_document: index.html
  error_document: index.html
  routes:
  - path: /
  source_dir: /
