domains:
- domain: app-state.aikon.one
  type: PRIMARY
name: aikon-app-stage
region: ams
static_sites:
- environment_slug: html
  github:
    branch: stage
    deploy_on_push: true
    repo: alexandervu/aikon_app
  name: aikon-app-stage
  routes:
  - path: /
  source_dir: /
